package basic_algorithm.array;

//滑动窗口问题
//给定大小的窗口从前向后移动
public class SlideWindow {

    //窗口的最大值
    //申请两个队列，分别保存数组元素和位置，遍历过程如下：
    //将元素队列中队尾不大于当前元素的所有元素依次出队，当前元素入队
    //若当前元素的位置与队首元素的位置之差等于窗口的大小，则队首元素出队
    //此时队首元素即为当前窗口中的最大值
    public static void generate(int [] array, int n) {

        return;
    }

    //窗口的中位数

}
